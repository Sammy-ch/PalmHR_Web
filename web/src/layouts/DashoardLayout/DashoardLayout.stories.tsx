import type { Meta, StoryObj } from '@storybook/react'

import DashoardLayout from './DashoardLayout'

const meta: Meta<typeof DashoardLayout> = {
  component: DashoardLayout,
}

export default meta

type Story = StoryObj<typeof DashoardLayout>

export const Primary: Story = {}
